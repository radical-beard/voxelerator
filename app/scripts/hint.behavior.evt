---
apis:
 - vox
register:
 - on_update
---
-- Context-sensitive hotkey hints, one quiet line above the status strip.
local acc = 0

function on_update(dt)
  acc = acc + dt
  if acc < 0.5 then return end
  acc = 0
  self.text = vox.hint()
end
