namespace Voxelerator.Mcp;

// voxelerator-mcp: stdio MCP server over the Voxelerator core library.
// Scaffold stub — the JSON-RPC loop and tool table land with the server.
public static class Program
{
    public static int Main(string[] args)
    {
        Console.Error.WriteLine("voxelerator-mcp scaffold — server not wired yet");
        return 0;
    }
}
